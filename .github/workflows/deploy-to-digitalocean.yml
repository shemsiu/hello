name: deploy to digital ocean
on: [push]
jobs:
  build:
    name: Build My Hello App
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: pwd

      - name: custom script
        run: sh ./scripts/runBash.sh
